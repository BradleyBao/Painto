<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Painto.ToolBarWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Painto"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winuiex="using:WinUIEx"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <winuiex:TransparentTintBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <!--<RowDefinition Height="50"/>-->
        </Grid.RowDefinitions>

        <!--<Canvas Grid.Row="0" Grid.Column="0" x:Name="DrawingCanvas" Background="Yellow" PointerPressed="Canvas_PointerPressed" PointerMoved="Canvas_PointerMoved" PointerReleased="Canvas_PointerReleased"/>-->
        
        <!--Old Polyline Logic with Canvas-->
        <!--<Canvas x:Name="DrawingCanvas" Background="Transparent" PointerPressed="DrawingCanvas_PointerPressed" PointerMoved="DrawingCanvas_PointerMoved" PointerReleased="DrawingCanvas_PointerReleased"/>-->

        <!-- Use Win2D CanvasControl for better performance -->
        <canvas:CanvasControl x:Name="MyCanvas" 
                              Background="Transparent"
                              ManipulationMode="None"
                              Draw="MyCanvas_Draw"
                              PointerPressed="MyCanvas_PointerPressed"
                              PointerMoved="MyCanvas_PointerMoved"
                              PointerReleased="MyCanvas_PointerReleased"/>
    </Grid>
    
    
</Window>
